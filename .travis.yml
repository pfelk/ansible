---
sudo: required

language: python
python: "2.7"

# Doc: https://docs.travis-ci.com/user/customizing-the-build#Build-Matrix
env:
  - ANSIBLE_VERSION=latest
#  - ANSIBLE_VERSION=2.9.0
#  - ANSIBLE_VERSION=2.8.0
#  - ANSIBLE_VERSION=2.7.0

branches:
  only:
    - master

before_install:
  - sudo apt-get update -qq

install:
  # Install Ansible.
  - if [ "$ANSIBLE_VERSION" = "latest" ]; then pip install ansible; else pip install ansible==$ANSIBLE_VERSION; fi
  - if [ "$ANSIBLE_VERSION" = "latest" ]; then pip install ansible-lint; fi

script:
  # Check the role/playbook's syntax.
  - ansible-playbook -i hosts deploy-stack.yml --syntax-check
  - ansible-playbook -i hosts roles/elasticsearch/tasks/main.yml --syntax-check

  # Run the role/playbook with ansible-playbook.
  - ansible-playbook -i hosts deploy-stack.yml -vvvv --ask-become deploy-stack.yml


